# ENTITY GRID runtime and development dependencies.
# Deliberately small: the power flow, the topology learner, the alignment
# search and the degradation detectors are all implemented directly rather
# than pulled in as libraries.
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
fastapi>=0.110
uvicorn[standard]>=0.29

# development
pytest>=8.0
httpx>=0.27
